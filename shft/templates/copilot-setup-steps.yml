name: "Copilot Setup Steps"

on:
  workflow_dispatch:
  push:
    paths:
      - .github/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/copilot-setup-steps.yml

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Set up Node.js
        uses: actions/setup-node@0d968d6c5c4916c61cde4112f99b4f9d5e1742b5 # v4.2.0
        with:
          node-version: "22"

      - name: Enable Corepack
        run: corepack enable

      - name: Install engine dependencies
        run: |
          cd .sandcastle/engine
          pnpm install --frozen-lockfile
