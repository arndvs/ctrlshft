name: "Agent: Implement PRD"

on:
  issues:
    types: [labeled]

concurrency:
  group: sandcastle-implement-prd-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  implement-prd:
    if: github.event.label.name == 'agent:implement-prd'
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: {{DEFAULT_BRANCH}}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install engine dependencies
        run: npm ci
        working-directory: .sandcastle

      - name: Run implement-prd workflow
        run: |
          npx tsx .sandcastle/run.ts \
            --workflow implement-prd \
            --issue ${{ github.event.issue.number }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
